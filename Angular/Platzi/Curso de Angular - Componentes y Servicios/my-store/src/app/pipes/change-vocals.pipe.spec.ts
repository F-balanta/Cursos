import { ChangeVocalsPipe } from './change-vocals.pipe';

describe('ChangeVocalsPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeVocalsPipe();
    expect(pipe).toBeTruthy();
  });
});
