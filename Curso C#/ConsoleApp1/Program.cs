using System;

namespace ConsoleApp1
{
    internal class Program
    {
        static void Main(string[] args)
        {
            float a = 7, b = -3, c = -9; 
            Console.WriteLine(Math.Sqrt(Math.Pow(a,2) + Math.Pow(b,2) + Math.Pow(c,2)));
        }
    }
}
