#include <stdio.h>

int main(void)
{
    int a;
    a = 5;
    a *= 5;//operador de asignacion sirve para acumular en la variable un valor. (+=, -=, /=, *=).
    printf("%d", a);
    
    return(0);
}