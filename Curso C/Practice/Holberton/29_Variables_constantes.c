#include <stdio.h>

// Una constante es un espacio almacenado en memoria que no cambia su valor
int main(void)
{
    const int x = 5; //Declarando una constante
    
    printf("% d", x);

    return(0);
}