#include <stdio.h>

int main()
{
     /*
    Para crear un archivo en c Utilizamos la siguente instruccion
    FILE * nombre_del_puntero
    *nombre_dek_puntero = fopen("Nombre Del Archivo", "wt")
    (con la W indicamos que vamos a crear un archivo y la "t" indicamos que el archivo será  de texto .txt))
    Si hay un archivo creado con erl mismo nombre este archivo sustituira su contenido.
     */
    return (0);
}