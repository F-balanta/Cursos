// querySelectorAll

//Tiene la misma sintaxis que querySelector pero se diferencian en que querySelector retorna un elemento que es el primero quee ncuentra
// mientras que querySelectorAll devolverá un arreglo con todos los elementos que cumplan la condicion del selector
// al valor que devuelve se le llaman Nodos o NotList
const div = document.querySelectorAll('div');
console.log(meta);

const cards = document.querySelectorAll('.card');
console.log(cards);
const formularios = document.querySelectorAll('#formulario');
console.log(formularios);
